关于etl的归一化处理
	数据转换：
		1，直接映射。
		2，字符串处理。
		3，字段运算。
		4，空值判断。
		5，日期转换。
		6，聚集运算。
		7，既定取值。

	关于我们的项目
		目前用到了
			直接映射--项目的初期大部分都是这样的
			字符串处理--这个是用的比较多的
			字段运算--这个在折扣的计算用到了
			空值判断--在判断是否是第三方时用到了
			既定取值--目前来看是没有的


关于品牌的统一处理：
	有一个简单的思路，获得所有的品牌的所有的可能，将之与一个标准品牌映射起来，这样，找到其中一种形式的就算是找到所有形式的了。
	难点有两个：
		一：如何获取所有的品牌。
		二：使用什么样的数据结构来构建这样的模型。

	关于第一个问题的解决办法：
		如何获取所有的品牌？
			我们每一次都获取一下品牌，不断的积累，将至存放在一个文件中。
			不断的不增加去重之后保留，这是一个不断积累的过程。

	关于第二个问题：
		使用map这种数据结构。
			map的key为不同的品牌，value为该品牌的一个唯一值。
			例如:	Map<String,String> brands = new HashMap<>();
					brands.put("apple","苹果");
					brands.put("Apple","苹果");
					brands.put("苹果/Apple","苹果");
					类似于这种形式的。
			将不同形式的品牌作为key，value作为为一个一个标示。

		接下来，如何判断两个不同形式的品牌是同一个品牌呢？
			例如：
				英国牛栏Nutrilon奶粉
				nutrilon/诺优能
				荷兰牛栏
				牛栏/Nutrilon
				牛栏（Nutrilon）
				牛栏(Cow&Gate)
				诺优能/Nutrilon
				以上是几个牌子呢？
				如何判断？

		ctrl+shift+C  注释xml文件中的一行
